<li>
    {{burger_name}}

    {{#if devour}}
    <button class="change-status" data-id="{{id}}" data-newstatus="{{devour}}">
        DEVOUR!!!
    </button>
    {{else}}
    <button class="delete-button" data-id="{{id}}" data-newstatus="{{devour}}">
        DELETE
    </button>
    {{/if}}

</li>